# CLAUDE.md — журнал изменений проекта Toilab

Этот файл ведётся автоматически: после каждого изменения сюда добавляется запись
(что и зачем меняли). Свежие записи — сверху.

## Текущее состояние

- Проект **полностью откатан к коммиту `b626b20`** («added moderating») — то есть к
  состоянию ДО редизайна. `git diff b626b20` пуст: `page.js`, `globals.css`, `layout.js`,
  `postcss.config.mjs` восстановлены из коммита; добавленные ранее компоненты
  (`src/app/(site)/_components/`) удалены.
- Остались только непривязанные (untracked) файлы, которых не было в `b626b20`:
  - `CLAUDE.md` — этот журнал;
  - `public/images/screens/` — реальные скриншоты приложения (`home.PNG`, `catelog.PNG`,
    `services.PNG`, `event.PNG`) + `README.txt`.
- Из песочницы файлы удалять нельзя (mount без unlink), поэтому полная зачистка untracked —
  только вручную на Mac: `git clean -fd` (⚠️ удалит и скриншоты).
- Запуск dev: `npm run dev` (порт 5001). Прод: `npm run build` затем `npm run start`.

## Журнал

### 2026-08-21 — занятые рестораны не предлагаются (пункт 9 аудита, мелочи)
- `restaurants-by-date`: живой пробой выяснена семантика — эндпоинт отдаёт рестораны, ЗАНЯТЫЕ на дату
  (после `POST /api/block` ресторан появляется в ответе). Формат `{date, restaurants:[…], total}`.
  `apiClient` +`getRestaurantsByDate`, `booking.js` +`busyRestaurantIds(dateISO)` → Set id.
- `checkBookingConflicts` переведён с `fetchAllBlockedDays` (выкачивал все блокировки) на этот запрос —
  точнее и легче. Автоподбор в мастере исключает занятые рестораны (deps эффекта +`date`), в модалке
  добавления услуги занятый помечен «Занят на эту дату», кнопка и поле количества скрыты.
- ℹ️ `PATCH /weddings/{id}/budget` и `/event-category/{id}/budget` — НЕ нужны: проверено вживую, что
  обычный `PUT` уже сохраняет бюджет (777 000 после PUT в обоих случаях). Пункт аудита оказался ложным
  пробелом, мёртвый метод не добавлялся.
- ℹ️ `/api/services` и `/api/placeholder-service` не переносились: в моб. объявлены, но нигде не вызываются.
- ✅ Проверено: блок на дату → автоподбор «Выбрано: 0»; смена на свободную дату → ресторан подобран
  (300 000 ₸); в модалке добавления — «Занят на эту дату». Все тест-данные и блокировки удалены.

### 2026-08-21 — «Оформление зала» и товары (пункты 7-8 аудита)
- **Оформление зала (hallDecorations)** — категория была в моб., в вебе отсутствовала:
  - `supplier.js` +группа (`/api/hall-decorations`, get/upd/del по id) — ключ в `/supplier/listings`
    называется `hallDecorations` (проверено живым ответом);
  - `categoryForms.js` +форма: salonName, decorationName, decorationType, cost, district, address,
    phone, description + обязательное фото (валидация из моб. Item2Screen.js:1011-1063),
    fileSegment `hall-decoration`;
  - `categories.js` +категория публичного каталога `/app/catalog/hall-decoration` (стало 17);
  - `catalogFields.js`: `decorationName` в NAME_FIELDS и в составное имя «Салон — Оформление»,
    подписи decorationName/decorationType, FILE_SEGMENT; `cart.js` SLUG_TYPE → `hallDecoration`;
  - `moderation/resources.js` +тип `hall-decoration` (иначе записи не видны модератору);
  - `supplier.js listingName` расширен (salonName/decorationName/studioName/teamName/itemName) —
    иначе карточка в кабинете показывала «—».
  - ✅ Проверено: категория в форме создания (17 в списке), POST /api/hall-decorations → 201,
    карточка в кабинете «Тест декор студия · 50 000 ₸ · На модерации», форма редактирования
    предзаполнена, DELETE 204. Тест-запись удалена.
- **Товары**: правка/удаление уже были доступны через общую форму; 🐞 найден баг — у товаров из моб.
  `category: 'Miscellaneous'` (кастомные подарки) нет в списке опций, селект был пуст и сохранение
  затёрло бы категорию. Теперь сохранённое значение показывается отдельной опцией (как у района).
  ✅ Проверено: правка товара (PUT /api/updategoodbyid/{id} → 200, cost и вложенные specs обновились),
  DELETE /api/removegoodbyid/{id} → 204. Тест-товар удалён.
- **Публичный wishlist**: `/app/wishlist/{id}?src=ec` теперь работает и для event-category
  (`/api/wishlist/public/eventcategory/{id}` + инфо из `/api/event-category/{id}`).
- ⚠️ Превью в этой среде периодически само перезагружается на `/app` в момент submit — из-за этого
  часть форм проверялась запросами к тем же эндпоинтам с теми же payload, что строит форма.

### 2026-08-21 — полное редактирование объявления: медиа, авто, номера (пункт 6 аудита)
- `supplier/edit/[group]/[id]/page.js` переписан (паритет с моб. ItemEditScreen):
  - **фото/видео записи**: текущие (`GET /api/{seg}/{id}/files` → миниатюры через `/toilab-api/`),
    пометка на удаление ×/↺ (`DELETE /api/files/{id}` при сохранении), добавление новых (`POST …/files`);
  - **авто салона** (transport): `GET /api/transport-vehicles?transportId=`, правка `PUT /api/transport-vehicles/{id}`,
    удаление, добавление (`POST`), фото `POST …/{id}/photos` (JSONB photos — его читает бронирование);
  - **номера гостиницы** (hotels): `GET /api/rooms/hotels/{id}/rooms`, правка `PUT /api/rooms/rooms/{id}`
    (room_type_id сохраняется), удаление, добавление (сначала `POST /api/rooms/room-types`, затем номер),
    фото `POST /api/rooms/rooms/{id}/photos`. Тип/вместимость берутся из `RoomType`.
  - Удаления отдельных фото авто/номера в моб. нет — не делаем. Все операции после PUT полей, по одному.
- `apiClient.js` +`deleteFile`, `updateVehicle`, `deleteVehicle`, `uploadVehiclePhoto`, `createRoomType`,
  `createRoom`, `updateRoom`, `deleteRoom`.
- 🐞 Поймано при проверке: чтение `e.target.files` внутри updater-колбэка `setState(fs => …)` — React
  выполняет его позже, когда `e.target.value = ''` уже очистил список → файл «не выбирался». Файлы
  захватываются ДО setState (в `supplier/new` так и было).
- ✅ Проверено (боевой бэкенд, ресторан 17 поставщика): форма предзаполнена, текущее фото видно; добавление
  тест-файла → `files` 2 (id 140); пометка × → сохранение → `DELETE /api/files/140`, снова 1 файл (исходный
  139 не тронут). Ветки авто/номеров живьём не проверены — у аккаунта нет салонов/гостиниц; контракты
  сверены с моб. ItemEditScreen.js:769-790, :849-898.

### 2026-08-21 — освобождение броней/дат при удалении мероприятия и услуги (пункт 5 аудита)
- Уточнение аудита: моб. Item3Screen НЕ показывает брони в деталке — оно ОТМЕНЯЕТ связанные брони
  номеров/авто при удалении категории (:2633-2657) и услуги (:2979), матчит по дате + room_ids
  (список броней не хранит ссылку на мероприятие). Веб делает так же.
- `booking.js` +`cancelBookingsForRows(rows, dateISO)`: `GET room/transport-availability/?date=YYYY-MM-DD`
  → отмена по `bookingReference` тех, чей roomId/vehicleId есть в room_ids услуг и status≠cancelled.
  `apiClient.getRoomBookings/getTransportBookings` приняли необязательный `query`.
- `events/[id]/page.js`: `rowsOf` отдаёт `room_ids`; `onDelete` и `removeItem` сначала освобождают брони и
  даты ресторанов (`DELETE /api/block`), затем удаляют. `events/page.js` `onDelete` — то же (подтягивает
  запись перед удалением). Всё best-effort.
- ✅ Проверено (ресторан; гостиниц/авто в Алматы на бэкенде нет): мероприятие с рестораном + блок даты →
  удаление из списка → all-blocked-days 0, мероприятий 0. Тест удалён.

### 2026-08-21 — гости мероприятия (RSVP), пункт 4 аудита
- `events/[id]/page.js`: блок «👥 Гости» (только организатору) — `GET /api/events/{wedding|eventcategory}/{id}/guests`
  → список «• Имя — N чел.» и «Всего гостей: totalPeople» (как alert в моб. Item3Screen:2043-2055).
  Грузится параллельно с wishlist.
- ⚠️ `eventType` строго `eventcategory` строчными: `eventCategory` бэкенд принимает с 200, но отдаёт пустой список.
- ℹ️ Гости отмечаются на серверной странице-приглашении (`/api/eventcategorywishes/{id}`, форма POST
  `/api/events/{type}/{id}/attend` с `name`, `guests_count`) — веб только показывает.
- ✅ Проверено (боевой бэкенд, тест-событие 13, удалено): RSVP «Тест Гость, 3» → блок «Всего гостей: 3 чел. | • Тест Гость 3 чел.».

### 2026-08-21 — тип мероприятия «Конференция» (пункт 3 аудита)
- `events.js` `EVENT_TYPES` +`conference` (storage eventCategory, type `conference`): транспорт, рестораны,
  торты, тамада, программа, оборудование, цветы, ювелирка — набор из моб. `ConferencesEventScreen.js:93-103`.
  Мастер, корзина, модалка добавления услуг и бейдж в списке подхватывают тип автоматически.
- ✅ Проверено (боевой бэкенд): тип в мастере с 8 категориями, автоподбор (ресторан × 40 = 400 000),
  создание → `event_kind: "conference"`, бейдж «КОНФЕРЕНЦИЯ» в списке. Тест-запись удалена.

### 2026-08-21 — добавление услуг в существующее мероприятие (event-category), пункт 2 аудита
- Только для мероприятий-«категорий» (`?src=ec`): у свадеб (weddings) на бэкенде нет эндпоинта
  добавления позиции (моб. `updateWedding` меняет лишь name/date) — кнопка у свадеб не показывается.
- Новый `_lib/AddServiceModal.jsx`: категории берутся из набора типа мероприятия по `event_kind`
  (`key` или `apiType`; неизвестный тип, напр. `conference` из моб., → все категории) → список услуг
  города (`fetchList` + фильтр по городу, как в мастере) с поиском → поле количества (для ресторана =
  гостей) → «Добавить». Гостиницы/транспорт — через существующие `RoomPickerModal`/`VehiclePickerModal`
  (room_ids = номера/авто, quantity = число номеров / дней). Уже добавленные помечены «✓ В мероприятии»
  (ключ `${serviceType}-${serviceId}`, как в `rowsOf`).
- `events/[id]/page.js`: кнопка «＋ Добавить услугу» (isCat && isHost) → `addService`:
  `checkBookingConflicts` (занятость ресторана/номеров/авто ДО добавления, как при создании) →
  `POST /api/event-category/{id}/service` → PATCH `total_cost`/`remaining_balance` (total += cost) →
  `createBookingsForWedding` (брони + блок даты ресторана, best-effort) → reload.
- При удалении ресторана из мероприятия теперь снимается блок его даты (`DELETE /api/block`) —
  иначе дата оставалась занятой и ресторан нельзя было добавить повторно.
- ✅ ПРОТЕСТИРОВАНО (боевой бэкенд, тест-событие id 11, затем удалено): добавление ресторана 20 гостей →
  `/service` 201, total 200 000/остаток 800 000; «✓ В мероприятии» в модалке; удаление → блок снят
  (all-blocked-days 0); повторное добавление 25 гостей → 250 000 без alert, блок поставлен; «чужой»
  блок на дату → alert «Занято на выбранную дату: Ресторан …», услуга НЕ добавлена. Тест-данные удалены.
- ℹ️ На боевом бэкенде в Алматы заполнена только категория «Рестораны» (1 запись) — остальные
  категории в модалке показывают «Поставщиков пока нет», это данные, а не баг.
- Не сделано из пункта 2: изменение количества у уже добавленной позиции (`PUT /services` заменой
  набора) — бэкенд не отдаёт cost по услугам, пересчёт сумм при замене набора был бы приблизительным.

### 2026-08-21 — фикс: кэш каталога и занятость ресторана при создании мероприятия
Починены два бага, найденные при проверке предыдущей задачи (правки только в вебе).
- **Кэш каталога не сбрасывался при смене города.** Списки в мастере создания уже отфильтрованы
  `filterCity`, но `cacheRef` не знал, какому городу принадлежит. После переключения города подбор
  считал по спискам прежнего города (например, после Шымкент → Алматы выдавал «Выбрано: 0», лечилось
  только перезагрузкой). Добавлен `cacheCityRef`; `openCategory` и `runAutoPick` выбрасывают кэш,
  если город сменился. ✅ Проверено: Шымкент (0 услуг) → Алматы → сразу «Выбрано: 1, 300 000 ₸».
- **Веб не проверял занятость ресторана на дату.** Моб. app перед созданием сверяется с blockedDays и
  не даёт создать мероприятие на занятую дату (CorporateEventScreen.js:3073-3086), веб — создавал, а
  последующий `POST /api/block` падал с 400 «Этот день уже заблокирован», причём молча. В
  `checkBookingConflicts` добавлена проверка через `fetchAllBlockedDays` (best-effort: сеть упала — не
  мешаем создавать), в `createBookingsForWedding` неудачная блокировка даты больше не проглатывается,
  а попадает в `failures`. ✅ Проверено: дата ресторана заблокирована → «Занято на выбранную дату:
  Ресторан Space lounge bar and karaoke», POST создания НЕ уходит. Тестовая блокировка снята.
- ℹ️ Сам эндпоинт `/api/block` исправен: `{restaurantId:17, date:"YYYY-MM-DD"}` → 201; 400 означает,
  что день уже занят (не баг контракта).

### 2026-08-21 — несвадебные мероприятия сохраняются как event-category (паритет с моб.)
⚠️ Условие пользователя: править МОЖНО только веб; мобильное приложение и бэкенд — не трогать (только чтение).
- **Проблема (аудит):** моб. app все несвадебные типы (корпоратив, выпускной, предсвадебная вечеринка,
  семейное торжество, конференция) создаёт через `POST /api/event-category`, а веб слал всё в
  `weddings/addwedding`. Следствие: мероприятия из моб. не видны в вебе и наоборот.
- `apiClient.js`: +14 методов event-category (create/get/list/update/delete, PATCH total_cost/paid_amount/
  remaining_balance/budget, add/update/remove services), `getEventGuests`, wishlist по `eventcategory`.
- `events.js`: у `EVENT_TYPES` поля `storage` ('wedding' | 'eventCategory') и `apiType`
  (у семейного `traditional-family`, как шлёт моб.); `SERVICE_TYPE` (slug → PascalCase),
  `ITEM_TYPE_BY_SERVICE_TYPE` (обратная карта), `buildEventCategoryPayload`, `buildCategoryServices`.
- `events/new/page.js` и `cart/page.js`: развилка сохранения. В корзину добавлен селектор типа мероприятия
  (раньше корзина всегда создавала свадьбу). `cart.js`: +`buildCartCategoryPayload`/`buildCartCategoryServices`.
- `events/page.js`: список = `getallweddings` + `event-categories` (Promise.allSettled, если один источник
  упал — показываем второй), сортировка по дате ↓, ключ строки `${_src}-${id}` (id источников пересекаются),
  ссылка на деталку с `?src=ec`, удаление по источнику.
- `events/[id]/page.js`: универсальная деталка через `rowsOf(ev, isCat)` — услуги, удаление позиции с
  пересчётом, правка (PUT), wishlist (`event_type: 'eventcategory'`), «Поделиться» → `/api/eventcategorywishes/{id}`.
- ⚠️ Контракты, выявленные ЖИВЫМИ запросами (отличаются от кода моб. app):
  - тип события хранится в `event_kind`; `GET /api/event-categories` отдаёт только свои записи (401 без токена);
  - `GET /api/event-category/{id}/services` возвращает `services: []` даже когда услуги есть → читаем `EventServices` из объекта;
  - у `EventServices` НЕТ ни `cost`, ни id строки → сумма считается «цена каталога × quantity», ключ строки = `${serviceType}-${serviceId}`;
  - удаление услуги: `serviceType` нужен КАК ЕСТЬ (`Restaurant` → 204); нормализация из моб. (`restaurant`) даёт 404;
  - часть detail-эндпоинтов отдаёт массив (`/api/restaurantbyid/{id}`) → в `resolveNames` берём `[0]`
    (из-за этого у ресторанов раньше не было названия и цены и в свадебной деталке).
- ✅ ПРОТЕСТИРОВАНО в браузере на боевом бэкенде (аккаунт поставщика id 3): создание корпоратива
  (`event-category` 201 + `/service` 201, event_kind corporate, quantity 30), список с бейджем «КОРПОРАТИВ»,
  деталка (10 000 ₸ × 30 = 300 000 ₸), удаление услуги (204 + PATCH total_cost/remaining_balance),
  правка (PUT 200), создание «Выпускного» из корзины (100 000 = 10 000 × 10 гостей), удаление мероприятий,
  регресс свадьбы (создание + деталка + удаление). Все тестовые записи удалены.
- 🐞 Найдено попутно, НЕ чинилось: (1) в мастере создания кэш каталога не сбрасывается при смене города —
  после переключения подбор использует старые/пустые списки; (2) `POST /api/block` (блок даты ресторана
  после создания) отвечает 400 — best-effort, на создание не влияет.

### 2026-06-06 — SEO / PWA / шрифты / lang (фронт; бэкенд и nginx НЕ трогали)
По итогам анализа toilab.kz/app сделаны фронтовые правки (без бэкенда/nginx):
- **SEO-инфраструктура**: добавлены `src/app/robots.js` (раньше `/robots.txt` отдавал 404)
  и `src/app/sitemap.js` (раньше `/sitemap.xml` — 404). Sitemap включает `/`, `/contacts`,
  `/privacy`, `/app` и все страницы категорий `/app/catalog/<slug>` (из `categories.js`).
  В robots закрыты приватные/требующие логина разделы (cart/events/supplier/login/register,
  /moderation, /toilab-api).
- **Метаданные**: в корневом `layout.js` — `metadataBase`, `title.template = "%s — Toilab"`,
  `description`, `keywords`, OpenGraph/Twitter (og:image = `/images/logo.png`), `viewport.themeColor`.
  Раньше у ВСЕХ страниц был одинаковый `<title>` без description/OG.
- **Per-route metadata для клиентских страниц**: `app/layout.js` переписан в серверный
  компонент (экспортит metadata), клиентская оболочка вынесена в `_lib/AppShell.jsx`.
  Добавлен `catalog/[category]/layout.js` c `generateMetadata` → у каждой категории свой
  `<title>`/description/canonical (напр. «Рестораны для тоя — Toilab»; через `title.absolute`,
  т.к. шаблон не доходит через промежуточный /app layout).
- **`<html lang>`**: теперь синхронизируется с переключателем языка через эффект в
  `AppContext.jsx` (раздел /app, kz по умолчанию) и в `(site)/layout.js` (лендинг).
- **Шрифты самохостинг**: Google Fonts `<link>` заменён на `next/font/google` (Roboto,
  subsets latin+cyrillic) — убран рендер-блокирующий запрос. Playfair Display удалён
  (нигде не использовался). `globals.css` body и инлайн-стили обёрток → `var(--font-roboto)`.
- **PWA manifest** (`src/app/manifest.json`): иконки `/web-app-manifest-{192,512}.png`
  раньше отдавали 404 — сгенерированы из логотипа (sips, padding на фоне `#F5F0E9`,
  в `public/`). Добавлены `id/start_url=/app/scope/description/lang/categories`, бренд-цвета
  (`theme_color #4A3F35`, `background #F5F0E9`), purpose any+maskable.
- ✅ Проверено локально (`npm run build` + `npm run start` :5001): build зелёный;
  `/robots.txt` и `/sitemap.xml` 200; `/app` title «Каталог услуг для тоя — Toilab» + og:image;
  категория «Рестораны для тоя — Toilab» + canonical; 0 ссылок на fonts.googleapis.com
  (woff2 из `/_next/static/media`); иконки PWA 192/512 → 200.
- НЕ закоммичено/не задеплоено (ждёт явного разрешения).

### 2026-06-05 — «Поделиться» использует серверную страницу приглашения
- `events/[id]/page.js` `shareEvent`: ссылка теперь `https://api.toilab.kz/api/weddingwishes/{id}` (готовая
  серверная страница-приглашение с подарками, как `Share.share` в моб. Item3Screen), вместо своей
  `/app/wishlist/{id}`. Подарки на ней — те же (через `/api/wishlist`).
- Web Share API на телефоне, копирование на десктопе.
- ✅ Проверено (agent-browser): клик «Поделиться» → копируется `https://api.toilab.kz/api/weddingwishes/128`;
  страница backend отдаёт 200.

### 2026-06-05 — редизайн главной /app под стиль мобильного приложения
- `app/page.js` пересобран: герой-блок с градиентом золото→коричневый (linear 135°), эйбрау «TOILAB · {город}»,
  крупный заголовок «Организуйте свой той», подзаголовок, кнопки «Мои мероприятия →» (клиент) и «🛒 Корзина»,
  декоративный орнамент-казан 🍲 (opacity .12) в углу — отсылка к стартовому экрану моб.
- Карточки категорий: иконка в скруглённом беж-тайле (60px, radius 18) + подпись, белые карточки с мягкой
  тенью; hover (через инлайн `<style>`): translateY(-3) + усиление тени + акцентная рамка + затемнение тайла.
- Экран выбора города освежён (иконка-казан, тень кнопок).
- ✅ Проверено (preview screenshot 1180px): герой+орнамент, сетка 16 карточек-тайлов, kz-локализация.
  Скриншот-инструменты в среде капризны (preview-вьюпорт иногда схлопывается; помогает явная ширина).

### 2026-06-05 — главная: убрана секция «Организуйте мероприятие» и категория «Алкоголь»
- `app/page.js`: удалён блок «Организуйте мероприятие» (поля Бюджет/Гостей + кнопка «Создать мероприятие»)
  и связанное состояние/стили. Остались баннер «Мои мероприятия», «Что вам понадобится?» и сетка категорий.
- `categories.js`: удалена категория `alcohol` (стало 16 категорий). Создание: автоподбор — из «Мои
  мероприятия», ручной набор — через корзину.
- ✅ Проверено (agent-browser): на `/app` нет «Организуйте мероприятие»/полей бюджета, нет «Алкоголь»; категорий 16.

### 2026-06-05 — модерация: «Все типы» (все категории), сортировка и поиск
- Запрос: при «Все типы» показывался только один тип (товары); нужна сортировка по алфавиту и
  по пользователям + поиск по всем полям.
- 🐞 Причина «только товары»: бэкенд на `GET /api/moderation/pending?type=all` отдаёт лишь один тип,
  а список не пагинировался (pageSize 20, без подгрузки). Фикс на фронте:
- `api/moderation.js` +`fetchPaged({type,status})` (постранично, pageSize 100, до total) и
  +`fetchAllEntries({type,status,counts})`: для `all` берёт `counts` и тянет КАЖДЫЙ тип с n>0
  отдельно (`?type=<type>`), объединяет — так в очереди видны все категории; фолбэк на обход
  `type=all`, если counts пуст. Для конкретного типа — полная пагинация (раньше было max 20).
- `ModerationQueueView.jsx` переписан: грузит весь список через `fetchAllEntries`; поиск
  (`TextField` с иконкой) по всем полям — тип/название/поставщик/email/id/дата/`JSON(data)`;
  сортировка кликом по заголовкам Название/Поставщик/Создано (`TableSortLabel`, `localeCompare('ru')`,
  дата — по времени), по умолчанию Создано ↓. Подзаголовок показывает «показано: N» при поиске/фильтре.
  Модерация записи убирает её из загруженного списка и уменьшает counts.
- ✅ Проверено: `eslint` затронутых файлов — 0 ошибок. ⚠️ Live-проверка в браузере не делалась
  (Chrome-расширение было недоступно); логика подтверждается тем, что выбор конкретного типа в
  дропдауне уже показывал записи (значит типы есть, ломался только агрегат `type=all`).

### 2026-06-05 — «Подробнее» у товаров в каталоге подарков + вложенные поля
- В модалке «Подарки из каталога» (`events/[id]/page.js`) у каждого товара кнопка «ⓘ Подробнее» →
  открывает общий `ServiceModal` (type 'good', seg 'good') поверх каталога (каталог z-index понижен до 90,
  ServiceModal 100). Кастомный инпут «Название подарка»/«Добавить» и заголовок «Список подарков» убраны ранее.
- `catalogFields.getSpecs`: разворачивает вложенные объекты (напр. `specs` у товаров — адрес/телефон),
  ссылки/почта кликабельны (как и было).
- ✅ Проверено (agent-browser): 45 кнопок «Подробнее»; клик → модалка товара поверх каталога; ссылка товара
  (rickowens.eu) рендерится как `<a target=_blank>`.

### 2026-06-05 — выбор подарков из каталога + «Поделиться» (как Item3Screen)
- Изучен моб. `Item3Screen.js`: подарки выбираются из каталога товаров (`getGoods`) тогглом
  (`handleToggleWishlistItem` → createWish `{good_id,event_id,event_type:'wedding'}` / deleteWish);
  «Поделиться» — `Share.share({url})` со ссылкой на публичный список.
- `events/[id]/page.js`: кнопка «🎁 Выбрать подарки из каталога» (только хост) → модалка списка `/api/goods`
  с поиском и тогглом (createWish/deleteWish по good_id; статус «В списке ✓»). Позиция wishlist содержит
  good_id+item_name+cost — сопоставление и цена в списке. Кастомный подарок по названию сохранён.
- «Поделиться»: `navigator.share({title,url})` (Web Share API) со ссылкой `${origin}/app/wishlist/{id}`,
  фолбэк — копирование в буфер.
- ✅ ПРОТЕСТИРОВАНО (agent-browser, своё событие): модалка 45 товаров → «Добавить» → wishlist 201, статус
  «В списке ✓», позиция в списке; toggle-off → DELETE wishlist 200; кнопка «🔗 Поделиться».

### 2026-06-05 — «Подробнее» у услуг в детали мероприятия + общий ServiceModal
- Модалка услуги (фото/видео + лайтбокс + описание + характеристики со ссылками) вынесена в общий
  компонент `_lib/ServiceModal.jsx`. `ITEM_TYPE_META` дополнен `seg` (сегмент файлов по item_type).
- `events/[id]/page.js`: при резолве услуг сохраняются полные объекты (`itemObjs`), у каждой услуги —
  кнопка «ⓘ Подробнее» → открывает ServiceModal. (Форма создания пока использует свою рабочую модалку.)
- ✅ ПРОТЕСТИРОВАНО (agent-browser): событие 120 → 5/5 услуг с «Подробнее»; клик по фото/видео → модалка
  (5 фото, цена, описание, характеристики); клик по миниатюре → лайтбокс «1 / 5», изображение 2000px.

### 2026-06-05 — модерация: фото/видео, все поля и время создания в карточке
- Запрос: на странице `/moderation` в модалке записи должны показываться вся информация, все поля,
  фото и видео записи, а также время создания.
- `resources.js` +`fileSegment(type)` (resourceType → сегмент файлов; отличия: suvenir→suvenirs,
  traditional-gift→traditionalgift).
- `api/moderation.js` +`getEntryFiles(resourceType, id)` → `GET /api/{segment}/{id}/files`,
  нормализует ответ (`[]`/`{data:[]}`); в моке/ошибке — пустой массив.
- `utils/format.js`: расширены `FIELD_LABELS` (имена/цены/параметры/флаги/служебные), `formatValue(value, key)`
  теперь форматирует ISO-даты (`*_at`), деньги (→ ₸), булевы (Да/Нет), массивы, пустые объекты (—);
  +`hrefFor(key,value)` (URL/email/ссылки) и `mediaUrl(path)` (через прокси `/toilab-api`).
- `EntryDetailDialog.jsx` переписан: галерея фото/видео (грид миниатюр, у видео значок ▶) + лайтбокс
  (стрелки ‹/›, счётчик, закрытие); «Создано» с фолбэком `entry.createdAt || data.created_at || data.createdAt`;
  таблица данных рендерит ВСЕ поля — вложенные объекты (напр. specs) построчно, ссылки/почта кликабельны,
  значения отформатированы. Диалог расширен до maxWidth="md".
- `ModerationQueueView.jsx`: колонка «Создано» с тем же фолбэком на `data.created_at`.
- ⚠️ Медиа грузятся при открытии модалки. Состояние загрузки — производное (`mediaState.key` vs `entryKey`),
  чтобы не нарушать правило `react-hooks/set-state-in-effect` (иначе падает `next build`).
- ✅ Проверено: `eslint` по всем затронутым файлам — 0 ошибок (вкл. правила React Compiler). Полный
  `next build` не запускался (лимит времени песочницы); перезапуск прод — `npm run build && npm run start`.

### 2026-06-04 — деталь мероприятия как в моб. app («Мои мероприятия»)
- `events/[id]/page.js` переписан под вид мобильного экрана: услуги **сгруппированы по категориям**
  (разделы-заголовки) с иконкой, **названием услуги** (резолв по item_id), ценой за единицу × кол-во,
  суммой строки и кнопкой удаления ×. Шкала бюджета (Потрачено %, Остаток, красная при перерасходе).
- `events.js` +`ITEM_TYPE_META` (item_type → {ru,kz,icon,detail(id)}): резолв названия услуги через
  detail-эндпоинт по id (restaurantbyid/cake/jewelry/transport-vehicles/… все из categories.js + vehicle).
- `apiClient` +`deleteWeddingItem`, `updateWeddingTotalCost`, `updateWeddingRemainingBalance`.
- Удаление позиции: DELETE /api/wedding-items/{id} → пересчёт total → PATCH total_cost+remaining_balance → reload.
- ✅ ПРОТЕСТИРОВАНО (agent-browser): событие 120 → разделы ШОУ-ПРОГРАММА/ТОРТЫ/ФОТО-ВИДЕО/ЮВЕЛИРНЫЕ,
  имена («Ринго — Кольцо золотое женское»), «97 654 ₸ × 4 = 390 616 ₸», шкала «Потрачено 707 735 (100%),
  Остаток −107 735»; удаление позиции на тест-событии → 2→1, 35 000→20 000, остаток пересчитан.

### 2026-06-04 — корзина: ручной набор услуг → оформление мероприятия
- Новый поток (альтернатива автоподбору): набираем услуги по каталогу в корзину, затем на `/app/cart`
  вводим название/дату/бюджет/гостей → POST addwedding.
- `_lib/CartContext.jsx` — состояние корзины (`items: [{slug,item,quantity}]`), persist в localStorage
  (`toilab_app_cart`); add/toggle/remove/setQty/clear/has/count. Провайдер обёрнут в `layout.js` внутри AppProvider.
- `_lib/cart.js` — `SLUG_TYPE` (slug→item_type), `lineQty` (ресторан/гостиница = гости), `cartTotal`,
  `buildCartPayload` (id/type/quantity/totalCost + total_cost/remaining_balance).
- Кнопки «В корзину/В корзине ✓» на карточках списка каталога (preventDefault внутри Link) и в детали услуги.
- Шапка: иконка 🛒 со счётчиком (бейдж). Главная: подсказка про корзину.
- `cart/page.js` — позиции (кол-во/удаление, ресторан/гостиница = «× N гостей»), итоги (сумма/остаток),
  форма (название*/дата/бюджет/гости), создание → addwedding → очистка корзины → /app/events.
  Деньги через `fmtMoney` (исправлен двойной ₸).
- ✅ ПРОТЕСТИРОВАНО (agent-browser): добавление 2 ресторанов+торт → бейдж 🛒 3 (persist между страницами);
  корзина: гости 40 применяются к ресторанам (Сумма 250 160), создание addwedding 201 → редирект, корзина
  очищена; формат ₸ одинарный.

### 2026-06-04 — локализация из файла переводов мобильного, kz по умолчанию
- Скопирован `src/constants/translations.js` мобильного → `_lib/translations.js` (~850 пар RU→KZ, плоский словарь).
- `AppContext`: язык по умолчанию **'kz'** (был 'ru'); `t(ru, kz?)` → на ru возвращает ru, на kz: явный kz,
  иначе `translations[ru]`, иначе ru. Добавлен `tr(value)` — перевод значений данных.
- `catalogFields`: `trVal(value, lang)` переводит значения характеристик в kz (по частям через запятую);
  применён в `getSpecs` (кроме денег/ссылок) и в подзаголовке карточек списка каталога. Хардкод «Город»
  в шапке локализован.
- ✅ ПРОТЕСТИРОВАНО (agent-browser): свежая сессия → ҚАЗ активен по умолчанию, UI на казахском
  («Қала/Іс-шаралар/Шығу», «← Мейрамханалар»); деталь ресторана kz: подписи И значения переведены
  («Сыйымдылығы 40 · Асхана Итальяндық · Аудан Медеу»); переключение на РУС → русский.

### 2026-06-04 — перевод всех полей детального просмотра
- Собраны реальные ключи полей всех 15 категорий (через API). В `catalogFields.FIELD_LABELS` добавлены
  недостающие переводы: pricePerHour, pricePerMinute, minBudget, minRentalHours, workingHours, plateNumber,
  features, equipment, isAvailable, hasSafetyInsurance, deliveryAvailable, setupServiceAvailable.
  pricePerHour/pricePerMinute добавлены в MONEY_FIELDS (формат ₸). Подписи ru/kz по выбранному языку.
- ✅ Проверено (agent-browser): транспорт-авто → «Тип авто/Вместимость/Цвет/Цена за час/Мин. аренда ч/
  Доступно» (было pricePerHour/minRentalHours/isAvailable); equipment/fireworks/program — сырых camelCase нет.

### 2026-06-04 — кликабельные ссылки в характеристиках (Подробнее/каталог)
- `catalogFields.getSpecs` теперь возвращает `[label, value, href]`: URL (http/https), поля-ссылки
  (link/website/portfolio/site/url/instagram, с авто-https) и email (→ mailto) делаются кликабельными.
  Денежные/обычные поля — как есть.
- Отрисовка в модалке «Подробнее» (`events/new/page.js`) и в карточке каталога: при href — `<a target=_blank
  rel=noopener>` (цвет акцентный, break-all), иначе обычный текст.
- ✅ Проверено (agent-browser): тамада → Портфолио `https://www.instagram.com/...` рендерится как ссылка
  target=_blank.

### 2026-06-04 — «Подробнее» в доске «Выбранные услуги»
- Кнопка «ⓘ Подробнее» добавлена и в строки сводки подобранных услуг (не только в аккордеон категорий) —
  открывает ту же модалку с описанием/характеристиками/фото-видео+лайтбокс. Есть у всех позиций, включая
  ресторан/гостиницу.
- ✅ Проверено (agent-browser): 9/9 строк сводки имеют «Подробнее»; клик → модалка с деталями услуги.

### 2026-06-04 — модалка «Подробнее»: лайтбокс (увеличение фото/видео)
- В модалке услуги миниатюры фото/видео стали кликабельными (cursor zoom-in, у видео — значок ▶).
  Клик → полноэкранный лайтбокс (`events/new/page.js`, z-index 200 поверх модалки): фото contain,
  видео с controls+autoplay, стрелки ‹/› для перелистывания, счётчик «i / N», закрытие по ×/фону.
- ✅ ПРОТЕСТИРОВАНО (agent-browser): клик по миниатюре → лайтбокс (1/3), ‹/› → 2/3, × → закрытие,
  модалка «Подробнее» остаётся открытой под ним.

### 2026-06-04 — подбор: кнопка «Подробнее» (модалка с описанием и фото/видео)
- В карточке услуги (`events/new/page.js`, аккордеон категории) добавлена ссылка «ⓘ Подробнее» →
  модальное окно: категория, название, цена, фото/видео (грузятся из `/api/{segment}/{id}/files`),
  описание, локализованные характеристики (переиспользован `getSpecs`/`fileUrl` из catalogFields).
  В модалке — кнопка «Добавить в подбор / Убрать», закрытие по фону/×. Без ухода со страницы.
- Сегмент медиа для транспорта = `transport-vehicle` (услуги транспорта — авто), иначе `FILE_SEGMENT[slug]`.
- ✅ ПРОТЕСТИРОВАНО (agent-browser): «Подробнее» у ресторана → модалка с 3 фото, ценой 5 500 ₸, описанием и
  характеристиками (Вместимость/Кухня/Адрес/Телефон/Район/Город); «Добавить в подбор» → закрытие + выбрано +1.

### 2026-06-04 — подбор: удаление позиции + ввод количества с авто-пересчётом
- В сводке «Выбранные услуги» (`events/new/page.js`) у каждой не-гостевой позиции теперь поле ввода
  количества (number) + кнопки −/+ и строка «цена × кол-во = сумма»; удаление позиции — ×.
  Ресторан/гостиница — «× N гостей» (кол-во = число гостей, фиксировано).
- Любое изменение (удаление, смена услуги в категории, количество) → авто-пересчёт суммы/остатка
  (useMemo по selected); в payload уходит totalCost = цена × количество.
- ✅ ПРОТЕСТИРОВАНО (agent-browser): цветы ×20 → строка 700 000, итог 1 217 000 → 1 882 000, остаток 118 000;
  удаление ресторана → 9→8 позиций, 1 882 000 → 1 132 000; добавление другого ресторана → 8→9, → 1 407 000.

### 2026-06-04 — UX создания мероприятия: этапы ввод → лоадер → результат
- `events/new/page.js` переработан в явный поток через состояние `phase` ('idle'|'loading'|'results'):
  - **idle**: подсказка «Укажите бюджет и количество гостей…» + ссылка «выбрать вручную»;
  - **loading**: карточка-лоадер со спиннером (CSS @keyframes tlspin) «Подбираем услуги под ваш бюджет…»,
    мин. время показа 800мс (не мигает), запускается через 450мс после ввода бюджета+гостей;
  - **results**: статус (в рамках/не хватает) + сводка «Выбранные услуги» + категории для ручной правки +
    закреплённые итоги и кнопка «Создать» (раньше всё показывалось сразу одним списком).
- Логика подбора прежняя (recommendSelection по долям бюджета); ручная правка отключает авто-пересбор,
  «Подобрать заново» — включает. Смена типа/бюджета/гостей (без ручных правок) → снова лоадер → пересбор.
- ✅ ПРОТЕСТИРОВАНО (agent-browser): idle-подсказка → ввод 2млн/50 → виден спиннер+«Подбираем…» → результат
  (9 услуг, 1 217 000, остаток 783 000) → создание addwedding 201.

### 2026-06-04 — «День рождения»: убраны 5 нерабочих категорий (откат паритета)
- По решению: 5 культурных категорий (нац.костюмы, музыканты, фотографы, видеографы, декор) УДАЛЕНЫ из веба —
  у них нет роутера/контроллера в бэкенде (`ToiBuisnessBackendThis/routes/` — нет файлов; `/api/musicians`
  и т.п. → 404), показывать пустые категории нет смысла.
- `EVENT_CATEGORIES`: удалены 5 noCatalog-записей. Набор `traditional` снова 9 рабочих категорий.
- `events/new/page.js`: убран мёртвый код noCatalog (guards в openCategory/runAutoPick, спец. пустое состояние).
- ✅ Проверено: «День рождения» → 9 аккордеонов (рестораны/гостиницы/тамада/программа/цветы/транспорт/торты/
  ювелирка/традиц.подарки).

### 2026-06-04 — «День рождения / семейное торжество»: паритет 14 категорий
- Добавлены 5 культурных категорий (как в моб. CreateTraditionalFamilyEventScreen): национальные костюмы,
  музыканты, фотографы, видеографы, декор. Набор `traditional` теперь 14 категорий — полный паритет с моб.
- ⚠️ У этих 5 НЕТ каталожных эндпоинтов на бэкенде (проверено: /api/national-costumes, /api/musicians,
  /api/photographers, /api/videographers, /api/decor и варианты → все 404). В мобильном они тоже пустые:
  `fetchData` традиц. экрана грузит только 9 стандартных + getEventCategories (это чужие события, не каталог),
  `data["national-costumes"]` и др. никогда не заполняются. Т.е. «как в мобильном» = видны, но без услуг.
- Реализация: в `EVENT_CATEGORIES` флаг `noCatalog:true` (list:null). В `events/new/page.js` — `runAutoPick`
  и `openCategory` пропускают такие категории (без 404-запросов), пустое состояние = «Поставщиков в этой
  категории пока нет». Автоподбор/`recommendSelection` их игнорируют (нет услуг с ценой).
- ✅ ПРОТЕСТИРОВАНО (agent-browser): «День рождения» → 14 аккордеонов; ввод бюджета/гостей → автоподбор 9 услуг
  (1 056 454/2 000 000), 404 по культурным категориям НЕТ; открытие «Музыканты» → заглушка.

### 2026-06-04 — карточки каталога: цена, фото, локализованные характеристики
- Новый `_lib/catalogFields.js`: `getName`/`getPrice` (поддержка всех полей — averageCost/pricePerDay/
  pricePerShow/cost/salonName/teamName/itemName и т.д.), `getSpecs(item,lang)` — локализованные подписи
  (Вместимость/Кухня/Средний чек/Район…), скрытие служебных полей (id, *_id, supplier_id, *_at, status),
  формат денег ₸, bool→Да/Нет; `FILE_SEGMENT` (slug→сегмент файлов) и `fileUrl(path)` (через /toilab-api прокси).
- `apiClient.js` +`getFiles(segment,id)` (GET /api/{segment}/{id}/files).
- `catalog/[category]/page.js`: цена теперь у всех категорий (была только price/cost → пусто у ресторанов и др.),
  показ района/адреса, ленивая загрузка первого фото в карточку (`CardImage`).
- `catalog/[category]/[id]/page.js`: фото/видео из эндпоинта файлов (раньше картинок не было — поля photos в
  записи нет), характеристики с локализованными подписями вместо сырых ключей, `supplier_id` и пр. скрыты.
- 🐞 Картинки: в записях нет инлайн-фото — отдаются отдельно `/api/{segment}/{id}/files` (`{path:"uploads/..."}`),
  URL через прокси `/toilab-api/{path}` (проверено: 200 image/jpeg).
- ✅ ПРОТЕСТИРОВАНО (agent-browser): рестораны — список с фото (14/14)+ценой(averageCost)+районом, деталь с 3 фото
  и подписями (Вместимость 40, Кухня Итальянская…); фейерверки (pricePerShow 50 000), фото/видео, типография, товары — цена/имя ок.

### 2026-06-04 — полный список типов мероприятий и их категорий
- Разобраны 5 экранов моб. app (HomeScreen, CorporateEventScreen, CreateTraditionalFamilyEventScreen,
  PreWeddingScreen, PromScreen) + navigation/index.js. Итоговые типы и наборы перенесены в `_lib/events.js`:
  - **Свадьба** (wedding): рестораны, цветы, торты, тамада, программа, ювелирка, традиц. подарки, транспорт, одежда.
  - **День рождения / семейное торжество** (traditional): рестораны, гостиницы, тамада, программа, цветы,
    транспорт, торты, ювелирка, традиц. подарки. (нац.костюмы/музыканты/фотографы/видеографы/декор — у них
    НЕТ каталожных эндпоинтов в api.js, в вебе не подключены.)
  - **Корпоративное** (corporate): транспорт, рестораны, гостиницы, торты, тамада, программа, аренда
    оборудования, ювелирка, цветы.
  - **Вечеринка перед свадьбой** (prewedding): транспорт, рестораны, гостиницы, торты, тамада, программа,
    оборудование, ювелирка, цветы, фото/видео, сувениры, типография.
  - **Выпускной** (prom): фото/видео, тамада, рестораны, транспорт, сувениры, фейерверки, оборудование, типография.
- В `EVENT_CATEGORIES` добавлены 5 категорий: `photo-video` (/api/photo-video-services, type photo-video),
  `fireworks` (/api/fireworks-services, costField pricePerShow), `suvenirs` (/api/suvenirs, type suvenir),
  `typography` (/api/typography, type typography), `equipment` (/api/technical-equipment-rentals,
  type technical-equipment-rental). `CATEGORY_WEIGHTS` дополнены. `catalogItemCost` — fallback +pricePerShow/pricePerDay.
- 🐞 Исправлен эндпоинт типографии: `/api/typographies` → 404; правильный `/api/typography` (отдаёт массив).
  Поправлено и в events.js, и в `categories.js` (каталог типографии тоже был сломан).
- ✅ ПРОТЕСТИРОВАНО (agent-browser): все 5 типов в селекторе; автоподбор «Выпускной» → 8 категорий
  (817 886/1 000 000), «Вечеринка перед свадьбой» → 12 категорий (1 504 938/3 000 000); создание prewedding →
  addwedding 201, деталь показывает все услуги с подписями (вкл. новые), удаление 200.

### 2026-06-04 — автоподбор услуг по бюджету (создание мероприятия)
- В `_lib/events.js`: `CATEGORY_WEIGHTS` (типовые доли: ресторан 40, тамада 10, цветы/программа/
  транспорт 8, торт/ювелирка 6, традиц. подарки 5, гостиница 16) и `recommendSelection(...)` —
  по одной услуге в каждой категории под целевую долю бюджета: внутри доли берём самый дорогой
  подходящий, иначе самый дешёвый; ресторан/гостиница считаются × число гостей.
- В `events/new/page.js`: автозапуск подбора (debounce 600 мс) при вводе бюджета И числа гостей,
  если пользователь не правил выбор вручную (`manual`). Подгружает списки всех категорий пресета,
  заполняет выбор. Сводка «Выбранные услуги» с ценами и удалением, кнопки «Подобрать заново»/
  «Очистить», статус (в рамках бюджета / не хватает). Ручной выбор по категориям сохранён ниже.
- ✅ ПРОТЕСТИРОВАНО (agent-browser, клиент): бюджет 1.5М/50 гостей → 7 услуг на 779 654 ₸ (остаток
  720 346); снижение бюджета до 150к → пересбор на 122 000 (адаптация к дешёвым вариантам);
  создание с автонабором → addwedding 201.

### 2026-06-04 — кабинет клиента: создание мероприятий + wishlist
- **`_lib/events.js`** — пресеты типов мероприятий (свадьба/корпоратив/традиционное; все шлют на
  один эндпоинт, отличаются набором категорий), карта категория→{type, costField, list} для items[],
  хелперы `buildWeddingPayload`/`buildItemsAndTotals`/`catalogItemName`/`catalogItemCost`/`fmt`.
- **`apiClient.js`** +методы: createWedding, getWeddings, getWedding, getPublicWedding, updateWedding,
  deleteWedding, createGood, createWish, deleteWish, getWeddingWishlist, getPublicWeddingWishlist,
  reserveWish, reserveWishByUnknown.
- **Страницы:** `events/page.js` (список мероприятий), `events/new/page.js` (тип → название/дата/
  бюджет/гости → выбор услуг по категориям с подсчётом бюджета → POST addwedding),
  `events/[id]/page.js` (инфо + услуги + wishlist + редактирование/удаление + ссылка для гостей),
  `wishlist/[id]/page.js` (публичный список подарков + резерв гостем без аккаунта).
- **Шапка/главная:** ссылка «Мероприятия» для клиента (roleId!==2), баннер + кнопка «Создать
  мероприятие» на `/app`.
- ⚠️ Контракт (проверено по коду + тестами):
  - Событие: `POST /api/weddings/addwedding` `{name, date:"YYYY-MM-DD", host_id, budget, total_cost,
    paid_amount:0, remaining_balance, items:[{id, type, quantity, totalCost}]}`. type ≠ slug:
    restaurant/cake/flowers/tamada/program/jewelry/traditionalGift/transport/clothing/hotel;
    цена restaurant/hotel → averageCost, иначе cost.
  - Список `GET /api/getallweddings`; деталь `GET /api/weddings/{id}` → `{data:{... WeddingItems:[{item_type,
    total_cost, quantity}]}}` (услуги в WeddingItems, НЕ items); update `PUT /api/updateweddingbyid/{id}`;
    delete `DELETE /api/weddings/{id}`.
  - Wishlist ссылается на товар: `POST /api/wishlist {event_id, good_id, event_type:'wedding'}`;
    кастомный подарок = `POST /api/goods {category:'Miscellaneous', item_name, cost:'0', supplier_id}` →
    взять id → createWish. Публично `GET /api/wishlist/public/wedding/{id}`; резерв гостем
    `PATCH /api/wishlist/{id}/reservebyunknown` тело `{data:{reserved_by_unknown:name}}`.
- ✅ ПРОТЕСТИРОВАНО через agent-browser (аккаунт клиента, боевой бэкенд): создание свадьбы с услугой
  (addwedding 201, бюджет считается), список, деталь (услуги из WeddingItems), добавление подарка
  (goods 201 → wishlist 201), публичная страница + резерв гостем (200, отражается у хоста и гостя),
  редактирование (PUT 200), удаление (DELETE 200).

### 2026-06-04 — полноценные формы создания по всем 16 категориям + медиа
- **Новый файл `_lib/categoryForms.js`** — спецификация форм по категориям (ключи как в
  SUPPLIER_GROUPS): поля (тип/обязательность/опции), сегмент пути для медиа `fileSegment`,
  билдеры payload (особые: goods→вложенный `specs`; program→`price`=cost; equipment→`website`=link),
  `formatPhone` (+7 (XXX) XXX-XX-XX), `validate`, `CITY_DISTRICTS`, `buildPayload`.
- **`supplier/new/page.js` переписан**: динамические поля под выбранную категорию, выбор фото/видео
  (multipart), репитер автомобилей для транспорта. Поток: POST create → `getEntityId` →
  загрузка файлов по одному `/api/{fileSegment}/{id}/files` (поле `file`) → для транспорта
  `POST /api/transport-vehicles` + файлы авто `/api/transport-vehicle/{id}/files`.
- **`apiClient.js`** +методы: `getEntityId`, `uploadListingFile(segment,id,file)` (Content-Type
  снимаем, браузер ставит boundary), `createVehicle`. Удалён мёртвый `buildCreatePayload` из supplier.js.
- ⚠️ Контракт бэкенда (выявлено тестами, важно — отличается от ожиданий):
  - Телефон шлётся ФОРМАТИРОВАННЫМ `+7 (XXX) XXX-XX-XX` (raw `+7XXXXXXXXXX` сервер отвергает).
  - **program**: обязателен `companyName` (НЕ teamName); поле названия → companyName.
  - **fireworks**: `eventType` обязателен; `duration` — целое число (мин).
  - **clothing**: `gender` строго `мужской`/`женский` (строчные).
  - **hotels**: `hotelType` enum [Отель/Хостел/Мотель/Гостевой дом/Апартаменты]; `rating` 1–5 (число).
  - **transport-vehicle**: обязательны `carType` (enum [Лимузин/Внедорожник/Седан/Минивэн/Кабриолет/
    Микроавтобус]), `carName`, `capacity`; цена авто → `pricePerDay`.
  - Сегменты медиа отличаются от create: `cake`, `good`, `traditionalgift`, `photo-video-service`,
    `fireworks-service`, `transport-vehicle`.
- ✅ ПРОТЕСТИРОВАНО сквозь agent-browser (build+start :5001, аккаунт поставщика, реальный бэкенд):
  все 16 категорий → POST create 201 + загрузка фото 201; транспорт с авто → +vehicles 201 +файлы авто 201.
  Записи ушли на модерацию. Тест-данные на боевом бэкенде (удалять вручную/модерацией).

### 2026-06-04 — роли поставщика и клиента на /app (этап 2)
- Роли по `roleId`: 1=админ, 2=поставщик, 3=клиент (как в моб. app).
- `_lib/supplier.js` — реестр 17 групп объявлений (ключи как в ответе
  `GET /api/supplier/listings`), пути create/delete, хелперы name/price/sub, buildCreatePayload.
- `apiClient.js` +методы: getSupplierListings, createListing, deleteListing.
- `register/page.js` — регистрация с выбором роли (Клиент=3 / Поставщик=2) → POST /api/register.
- `supplier/page.js` — кабинет поставщика: объявления со статусами (pending/approved/rejected +
  причина отклонения), удаление; guard по roleId===2.
- `supplier/new/page.js` — форма добавления объявления (категория + общие поля) → POST create,
  payload с алиасами имени/цены под разные ресурсы; город из контекста.
- Шапка: ссылка «Кабинет» для поставщика, «Регистрация» рядом со входом; баннер поставщика на главной.
- Клиент (roleId 3) = построенный каталог. Проверка ролей в браузере — далее.

### 2026-06-04 — веб-версия мобильного приложения (этап 1: ядро)
- Изучено мобильное приложение `/Users/billionare/Documents/DEVELOPMENT/REACT/ToiBuisness/testExpo53/my-js-project`
  (Expo/RN, Redux, бэкенд `https://api.toilab.kz`, 17 категорий услуг, роли, i18n). Ничего в нём не менялось.
- Создан раздел веб-версии в `src/app/app/` (маршрут `/app`):
  - `_lib/apiClient.js` — axios к api.toilab.kz, токен в localStorage, заголовок `x-city`; методы login/register/getUser/fetchList/fetchOne.
  - `_lib/categories.js` — реестр 17 категорий (slug, ru/kz, иконка, путь списка и детали).
  - `_lib/AppContext.jsx` — контекст: город (Алматы/Астана/Шымкент), язык ru/kz, авторизация (token+user) с persist.
  - `layout.js` — оболочка с шапкой (логотип, выбор города, язык, вход/профиль).
  - `page.js` — главная: выбор города → бюджет/гости → сетка 17 категорий.
  - `catalog/[category]/page.js` — список услуг категории (нормализация ответа, фильтр по городу).
  - `catalog/[category]/[id]/page.js` — карточка услуги (галерея, цена, описание, доп. поля).
  - `login/page.js` — вход (POST /api/auth/login).
- Этап 1 = вход + город + каталог + карточки. Дальше (по согласованию): создание мероприятий, кабинеты поставщика/админа, wishlist, регистрация.
- ТЕСТ в браузере: `/app` рендерится (шапка, город, бюджет/гости, сетка 17 категорий), переходы работают.
  Запрос к API упал с CORS (preflight `OPTIONS /api/restaurants` → 204, но GET с заголовком `x-city`
  заблокирован браузером — бэкенд не разрешает кросс-домен с localhost).
- ФИКС: добавлен same-origin прокси в `next.config.mjs` (rewrite `/toilab-api/:path*` →
  `https://api.toilab.kz/:path*`), `apiClient` baseURL = `/toilab-api`. ВАЖНО: rewrites
  применяются только при ПЕРЕЗАПУСКЕ dev-сервера.
- ВАЖНО: `apiClient` НЕ использует env `NEXT_PUBLIC_API_BASE_URL` (он = api.toilab.kz для
  модерации и перебивал прокси) — baseURL жёстко `/toilab-api`.
- Запуск через computer-use: создан `_run-prod.command` (kill :5001 → build → start),
  запускается двойным кликом из Finder (в Терминал печатать нельзя — tier "click").
- ✅ ПРОТЕСТИРОВАНО (npm run build + npm run start, порт 5001, проверка через браузер):
  главная /app (17 категорий) → каталог `/app/catalog/restaurants` подгружает реальные
  рестораны Алматы через `/toilab-api/...` (200) → карточка `/app/catalog/restaurants/2`
  («Вкусно и точка», поля из бэкенда). CORS решён прокси.
- TODO дальше: цена в карточке/списке (поле `averageCost` для ресторанов), изображения
  (у части записей нет URL), регистрация, создание мероприятий, кабинеты поставщика/админа.

### 2026-06-03 (после отката — добавление скриншотов)
- Добавлен компонент `PhoneFrame` (inline-стили, рамка телефона) прямо в `page.js`.
- Секция «Как выглядит приложение»: вместо одиночного `app.png` — ряд из 4 скринов
  (`home/catelog/services/event.PNG`) в рамках телефонов + подписи. Данные в `t.screenshots.shots`.
- CTA-секция «Скачайте Toilab»: убран старый `logo3.png` (запечённые экраны входа),
  вместо него 2 реальных скрина (`home`, `services`) в рамках телефонов.

### 2026-06-03
- **Откат проекта к `b626b20`** по просьбе пользователя: восстановлены 4 изменённых файла
  из коммита перезаписью (`git show b626b20:<file> > <file>`), т.к. `git reset/stash`
  в песочнице падают (нет прав на удаление файлов). Код идентичен оригиналу.
- Попытка `npm run start` — упала: нет production-сборки (нужен `npm run build`).
- Создан CLAUDE.md для ведения журнала.
- (Отменено откатом) Редизайн «премиум-этно»: hero с телефоном, лента 17 категорий,
  стат-плитки, выноски, реалистичная рамка телефона, шрифт Montserrat, токены палитры.
